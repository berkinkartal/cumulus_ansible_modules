
# Cumulus NTP Role

Configure Network Time Protocol

Variable | Choices/Defaults | Type
--- | --- | ---
ntp.timezone|timezone the switch ntp resides in.|String
ntp.server_ips|list of all ntp server ips.|List of Strings

## Examples
{% for inventory in list_of_inventories.stdout.split('\n') %}
<details><summary markdown="span">{{ inventory }}</summary>
{%   for switch in hosts_per_inventory.results|selectattr('item', 'defined')|map(attribute='stdout_lines')|first %}
{{ switch }}
<pre><code>ntp:
{{ hostvars[inventory_hostname][inventory + '_' + switch]["ntp"]|default('N/A')|to_nice_yaml( width=50 )|trim|indent(2, indentfirst=True) }}
</code></pre>
{%   endfor %}
</details>
{% endfor %}
