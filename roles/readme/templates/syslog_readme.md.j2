
# Cumulus Syslog Role

Configure Syslog

Variable | Choices | Type
--- | --- | ---
syslog.servers|list of syslog servers.|List of Strings

## Examples
{% for inventory in list_of_inventories.stdout.split('\n') %}
<details><summary markdown="span">{{ inventory }}</summary>
{%   for switch in hosts_per_inventory.results|selectattr('item', 'defined')|map(attribute='stdout_lines')|first %}
{{ switch }}
<pre><code>syslog:
{{ hostvars[inventory_hostname][inventory + '_' + switch]["syslog"]|to_nice_yaml( width=50 )|trim|indent(2, indentfirst=True) }}
</code></pre>
{%   endfor %}
</details>
{% endfor %}
